# Klimr — Audit Remediation Status (KCDX ledger)

**Generated:** 2026-08-07 from `Klimr_Codex_Findings.json` (SHA-256 verified against manifest).  
**Registry:** 68 findings — 8 P0 · 50 P1 · 10 P2 · 0 P3. Trace IDs immutable.  
**Production migration baseline:** 0234 (owner-confirmed 2026-08-10). At audit generation it was 0188 (owner-confirmed 2026-08-07), which is the baseline the Tier-1 reconfirmation notes below were run against. Every "awaiting 0NNN paste + deploy" disposition below is now complete on **both halves** — migrations applied through 0234 and the matching code batch deployed on Vercel (owner-confirmed 2026-08-10). Per-row evidence labels are unchanged: a finding still closes only via its Definition-of-Done with real-environment (Tier-2/production) evidence where marked.  
**Dispositions:** `OPEN` · `FIXED AND VERIFIED` · `FEATURE DISABLED` · `ACCEPTED RISK` · `DEFERRED` · `NOT APPLICABLE` · `DISAGREED WITH EVIDENCE`.  
**Rule:** a finding leaves OPEN only when its Definition-of-Done row (see program doc) is complete with real-environment evidence.

**Reconfirmation provenance.** Notes in the last column come from the Tier-1 replay cluster (schema, RLS policies, grants, function bodies). Tier-1 proves what the catalog says; it does NOT prove behaviour at the real PostgREST/Storage/Realtime boundary. Every reconfirmation marked `(replay)` is therefore provisional and must be re-proven at Tier 2 with actual anon/member/organizer/admin/service clients before its finding can close (working rules 4 and 9).

| KCDX | Sev | Batch | Subsystem | Title | Aliases / Prior | Disposition | Reconfirmation (this session) |
|---|---|---|---|---|---|---|---|
| KCDX-001 | P0 | C1 | Privacy / Profiles / RLS | Every member can retrieve other members' exact DOB, phone, and home area | SECDB-001 / S2-SEC-001 | FIX BUILT — awaiting 0191 paste + deploy | 0191 revokes table-level SELECT on `profiles` from anon/authenticated and grants back 30 named public columns; twelve private columns are unreadable member-to-member by any direct path. `profile_private` (own row, auth.uid()-filtered, definer) and `profiles_public` (approved projection, invoker) restore legitimate access; generated `is_active` replaces raw account state. **Acceptance test run with real Postgres roles: 11/11** — member A gets `permission denied for table profiles` on B's DOB/phone/ZIP/neighborhood/account_status including `select *`; B's public projection works; A's own private row returns values; A reading B via profile_private returns 0 rows; own writes still work and stay row-scoped. 31 call sites migrated; 3 cross-member leaks (invites, network, feed) lost the column; 2 legitimate server-side needs (opponent scoring, AI availability search) moved to privileged reads returning only derived signals. Boot sentinel now fails a deploy if the table grant is restored. **Note: an initial column-level REVOKE was ineffective** (Postgres treats column privileges as additive to a table grant) — caught by the role test, not by the build. Follow-ups recorded: first_name/last_name still readable; `app/search/actions.ts` neighborhood hydration via admin client is KCDX-026. |
| KCDX-002 | P0 | C2 | Queue / Realtime / RLS | Raw Queue presence and join requests are anonymously readable and streamed | SECDB-002,RP-003 / S2-SEC-002,S2-SEC-003 | FIX BUILT — awaiting 0192 paste + deploy | 0192 revokes SELECT on all five queue tables from anon+authenticated, drops their `USING (true)` policies, and removes them from `supabase_realtime`. Role-tested: anon and member both get `permission denied for table queue_teams` / `queue_team_members`; publication now contains only match_participants and messages. No app regression — every queue read already went through `loadSessionState()` on the service role, and the browser hook already polled at 3s alongside the subscription (now removed). |
| KCDX-003 | P0 | C3 | Tournaments / Authorization / Data integrity | Tournament RLS permits direct hosting, payment confirmation, roster injection, and cross-binding | SECDB-003 / — | FIX BUILT — awaiting 0193 paste + deploy | 0193 revokes INSERT/UPDATE/DELETE on the three self-service tables from anon+authenticated and replaces them with four transactional SECURITY DEFINER commands. Role-tested: a member inserting a registration with status='confirmed'/payment_status='confirmed' gets `permission denied`; updating their own entry to paid gets `permission denied`; forging a confirmed payment row gets `permission denied`. Via the commands: register → pending; register again → already_registered; second member at capacity 1 → waitlisted (counted under a row lock, closing the TOCTOU for this path); unknown tournament → not_found; submit proof → payment row written as 'submitted' with a server-computed amount; registrant approving their OWN payment → not_allowed; owner approving → confirmed; withdraw is idempotent. `status`/`payment_status` are not parameters of any command. App wired: solo + team registration, proof submission, and confirm/deny/refund all go through the commands; staff/system paths (waitlist reorder, team roster snapshot, substitution) use the privileged client with stated reasons. Boot sentinel fails a deploy if the direct-write grants return. Not folded in, by design: KCDX-045/046/047 concurrency (batch E). |
| KCDX-004 | P0 | B | Deployment / Database contract | Migration ledger is contradictory and deployed code depends on ledger-pending objects | SECDB-004,RP-004 / S2-DEP-001,S2-QUEUE-001,S2-REL-001,S2-REL-003 | **FIXED AND VERIFIED** | Baseline 0188 established; six drift objects found and recorded (avatar_path; both 0188 function bodies — CRLF + a better `s.metric` fix; seed ordering; avatars bucket limits; four duplicate dashboard Storage policies; the hand-applied anon DML revoke). 0189 applied by Gabriel 2026-08-07; **all fifteen catalog measures then matched a clean replay** (n_policies 262→258, fp_policies →214c6de4). 0190 applied: `schema_manifest_missing()` verifies tables, functions AND service_role EXECUTE — proven against a deliberately broken DB. Sentinel un-gated from Vercel (build-phase gate instead) so every runtime checks. Both replay gates now run in CI on postgres:17. Residual: harness is PG16 locally; production 17.6. |
| KCDX-005 | P0 | C4 | Feed / Moderation | Approved Feed posts can be changed without re-moderation | SSF-FEED-001 / — | FIX BUILT — awaiting 0194 paste | 0194 adds three BEFORE/AFTER triggers so any moderation-relevant edit atomically returns the item to `pending` and clears stale labels: `posts_remoderate_on_edit` (body, media_path, duration, milestone, match_summary, post_type, repost_of), `post_comments_remoderate_on_edit` (body), `post_media_remoderate_parent` (attaching or removing media on an approved post). A caller that explicitly sets a status in the same statement is respected — that is the re-screen pipeline — and the author cannot use that door because `guard_moderation_update` rewrites their attempted status first. Role-tested end to end: approved post + feed row → author edits body → pending AND feed row withdrawn → author self-approve attempt → still pending → re-screen approves → approved and re-emitted → author attaches media → pending and withdrawn again. **Two pre-existing defects repaired in the same migration:** `feed_post` was `AFTER UPDATE OF moderation_status`, which never fires for `set body = …`, so Feed withdrawal had never worked for an edit; and `guard_moderation_update` reverted status changes made from inside our own triggers (current_user is the definer). |
| KCDX-006 | P0 | C5 | Feed / Media safety | Unscreened and client-asserted video can publish as approved | SSF-FEED-002 / S2-SEC-006 | **FEATURE DISABLED** (containment) — finding remains OPEN per working rule 2 | Root cause confirmed precisely: `createPost` ran the classifier for images only; for video it pushed the LABEL `media_unscreened` and no verdict, so `flagged`=false, `gateDown`=false and status computed to **approved**. Duration came from a browser `<video>` probe and content type from the upload request — both client-asserted, never checked against bytes. 0195 contains it at two boundaries: `feed-media` MIME allowlist drops video/mp4|webm|quicktime (Storage refuses the bytes) and `posts_reject_video` trigger refuses the row. Role-tested: member REJECTED, **service_role REJECTED** (no privileged bypass), duration on a non-video post REJECTED, ordinary post unaffected. Already-approved video posts returned to `pending` (nothing deleted). Composer tab removed and both server paths refuse — UI last and least, per rule 2. Boot sentinel fails the deploy if the trigger goes or the MIME types return. **Still owed before this closes:** server-side byte/type validation, duration+codec probing, moving-image moderation policy, private staging, derivatives, captions, quotas, cleanup, hostile-file tests. Re-enable = drop the trigger + restore the MIME types, as the LAST two lines of the migration that ships the gate. **Owner decision (recorded, consistent with the CSAM seam built in Jun 2026): prohibited-content screening is delegated to an external provider behind a fail-closed webhook contract — we do not build frame sampling.** That covers screening only; byte-level type validation, duration/codec probing from the file, private staging, derivatives, captions, quotas, cleanup and hostile-file tests remain ours. Split documented in SAFETY.md. |
| KCDX-007 | P0 | C2 | Queue / Courtside authorization | The public Queue join code authorizes service-role match operations | RP-001 / S2-SEC-002 | FIX BUILT — awaiting 0192 paste + deploy | `gameOverByCode` / `startNextByCode` / `stepDownByCode` now require a proved Courtside capability via `courtside_authorize(install_id, token_hash, session_id)` — scoped to one session, honours revocation, expires after 10 minutes of silence. The token primitive already existed (0180/0184, SHA-256 stored); it simply was never demanded. The code still selects WHICH session and no longer grants permission to change it. Kiosk sends installId + deviceToken on every command and surfaces refusals. |
| KCDX-008 | P0 | C2 | Queue / Projection / Secrets | Public Queue rendering discloses the intended operator credential | RP-002 / S2-SEC-002 | FIX BUILT — awaiting 0192 paste + deploy | Two layers. DB: `court_sessions` table SELECT revoked, public columns granted by name — `code`, `display_code`, `center_lat`, `center_lng`, `organizer_id` withheld (role-tested: member gets permission denied). App: `projectQueueState` gains a fourth audience — a registered display proves a capability via `x-klimr-install`/`x-klimr-device-token` headers and receives `displayCode`; everyone else gets null. QueueAudience and the ETag key extended so a player snapshot can never be served to an operator or vice versa. **Reverses the Phase 0 decision** that display_code was an acceptable public credential, including the test that asserted it. |
| KCDX-009 | P1 | D3 | Business accounts / Verification | Business verification tier is attacker-controlled at insert and survives activation | SECDB-006 / — | **FIXED** — awaiting 0201 paste | `guard_business_protected` was BEFORE UPDATE only; the INSERT policy checked owner+draft and said nothing about tier, so a member could create a business already claiming tier2 and the guard then protected that value forever. Now fires on INSERT too, forcing `verification_level='none'`, `status='draft'`. Existing unearned tiers reset to none (none deleted; a genuinely reviewed tier is re-granted by review). Role-tested: a member inserting tier2/active gets none/draft. **Two live bugs found while testing and fixed in the same migration:** (1) `businesses readable` recursed with `business_members` → 42P17 on any `INSERT ... RETURNING`, and `app/business/actions.ts` swallows it via `if (!inserted) return;` — replaced the correlated subquery with the existing SECURITY DEFINER `is_business_manager()`; (2) underneath that, RETURNING requires the SELECT policy to pass but ownership is written by an AFTER INSERT trigger, so the creator could not see their own new draft — added the `owner_id = auth.uid()` arm. Business creation had never worked end to end. |
| KCDX-010 | P1 | D3 | Teams / Invites | Mutable team invites can be rebound and accepted through a service-role write | SECDB-007 / — | **FIXED** — awaiting 0202 paste | The invitee's UPDATE policy pinned only `invited_user_id` in both USING and WITH CHECK, so `team_id` was rewritable; `respondTeamInvite` then fed that value to a service-role `team_members` upsert. Policy dropped; `team_invite_respond()` resolves the invite under `FOR UPDATE` from its id alone and never accepts a team id from a caller, with the capacity count inside the same transaction as the insert. `freeze_invite_identity` trigger pins team/invitee/inviter even for service_role. Role-tested end to end: A holds an invite to Team One, PATCHes `team_id` to a team they were never invited to, accepts — invite team_id unchanged, A lands in Team One only; second accept returns `already_answered`. |
| KCDX-011 | P1 | D3 | Professional verification | Professional verification approval races applicant-controlled state | SECDB-008 / — | **FIXED** — awaiting 0203 paste | The applicant had whole-row UPDATE with only `user_id` pinned, so `role` and every credential field stayed editable while an admin reviewed; approval re-read the row and granted whatever it then said. 0203: (1) freeze — reviewable fields of a submitted application are immutable to the applicant, with withdraw as the visible escape hatch; (2) hash — `content_hash` over the reviewed content, and `provider_review_decide()` approves only a matching version, returning `changed_since_review` otherwise, and RETURNS the reviewed role so the caller grants what was judged rather than re-reading. Admin console renders the hash into the form. Role-tested: applicant swap mid-review leaves `role=coach`; approve with reviewed hash → STORED status `approved`; replay → `not_pending`; a service-role edit then a stale hash → `changed_since_review`, status stays pending. **Systemic fix included:** `is_privileged_writer()` replaces `current_user <> 'service_role'` in the guards, which is wrong inside SECURITY DEFINER functions and had silently discarded three of our own writes today (0194 re-moderation, 0200 recompute, 0203 approval). |
| KCDX-012 | P1 | D3 | Marketplace / Offers | Marketplace offers and meetups do not bind all participants or parent rows | SECDB-fam / — | **FIXED (offers)** — meetups still owed | `loadContext(listingId, buyerId)` took the buyer from the CALLER and authorized with `user.id !== buyerId && user.id !== sellerId` — a seller passes that for any buyerId, so an offer could be attributed to a buyer who never made one, bound to a conversation that is not their thread. `marketplace_offer_create()` derives everything from locked canonical rows: buyer from the caller, or for a seller's counter from the parent offer; conversation from `conversations (listing_id, created_by)`, which is unique. Parent offers are validated to belong to the same listing and buyer. `listing_offers_freeze_identity` pins listing/buyer/actor/amount/parent after insert. Role-tested: a seller calling create with no parent gets `seller_needs_parent`. **Still owed:** the meetup rows in the same finding are a separate surface and are not covered here. |
| KCDX-013 | P1 | D3 | Classes / Enrollment integrity | Class enrollment RLS lets learners self-assert payment and attendance | SECDB-009 / — | **FIXED** — awaiting 0201 paste | `class_enrollments_update` let `user_id = auth.uid()` update the whole row, including `payment_status` and a `status` whose values include `attended`/`no_show` — the provider's observations. Column grants cannot separate learner from provider (both are `authenticated`; the difference is per-row), so writes became commands: `class_set_confirmation()` (the learner's genuine decision) and `class_cancel_enrollment()` (either party). Learner UPDATE policy replaced with provider-only; a `guard_enrollment_insert` trigger pins status/payment/confirmed_at on insert so the identity-only INSERT policy cannot be used to self-assert either. Structural fix for the missing binding: composite FK `(session_id, class_id)` → `class_sessions(id, class_id)`, with mismatched rows repaired to the session's class first. Role-tested: learner enrolling as attended/paid stores enrolled/not_required; learner self-marking paid changes nothing; learner confirmation succeeds; provider attendance succeeds. |
| KCDX-014 | P1 | D2 | Storage / Media security | Direct Storage writes bypass media validation, moderation, and quotas | SECDB-010 / S2-SEC-006,S2-FEED-010 | **MOSTLY FIXED** — one browser path outstanding | Owner decision recorded: prohibited-content screening is delegated to a third-party provider for photos as well as video. That is only a control if every byte reaches it — and four buckets (`feed-media`, `avatars`, `listing-photos`, `credential-docs`) carried own-folder INSERT policies letting a member PUT with just their JWT, bypassing the type checks, hourly ceilings and CSAM seam entirely. 0199 drops those policies plus the redundant `bdocs insert`; the four upload paths move to server-minted signed tokens or server-performed writes (a signed token does not consult object policies, so uploading still works — uploading *unasked* does not). Bucket limits added where missing: listing-photos 5 MiB, credential-docs 10 MiB. `media_integrity_intact()` fails the boot if a member write path returns or a member-facing bucket loses its limits. **Outstanding, named in the migration:** `tournament-payments` still accepts a browser upload (`payment-proof-upload.tsx`), so `tpay insert` remains; bounded by bucket limits and by 0199 removing UPDATE/DELETE, but converting it to a server-minted token is the last piece. |
| KCDX-015 | P1 | D2 | Evidence integrity / Storage | Credential, business, and tournament evidence can be replaced after review | SECDB-011 / S2-SEC-006 | **FIXED** — awaiting 0199 paste | `credential-docs`, `business-docs` and `tournament-payments` let the submitter UPDATE or DELETE the object at the path a reviewer had approved — the decision row still reads 'verified' while pointing at different bytes. Not an exotic race: the ordinary shape of 'get approved with a real document, then replace it'. 0199 drops `credential docs delete own`, `bdocs delete`, `tpay update`, `tpay delete`. INSERT stays deliberately — submitting NEW evidence appends rather than rewrites, and a review decision is bound to the row it judged. Deleting evidence becomes a service-role operation, which is where a retention or legal-hold decision belongs. Asserted by `media_integrity_intact()`. |
| KCDX-016 | P1 | D1 | Database privileges / Defense in depth | Blanket grants and implicit PUBLIC EXECUTE amplify every policy/function mistake | SECDB-012 / S2-SEC-003,S2-SEC-009 | **FIXED** — awaiting 0196 paste | 0196: TRUNCATE/REFERENCES/TRIGGER revoked from anon+authenticated on every relation (views included); anon SELECT revoked everywhere except the 8 tables it genuinely reads, derived from the live policy set so no behaviour changes; implicit PUBLIC EXECUTE revoked with authenticated+service_role granted explicitly so member access is unchanged; schema default privileges narrowed so new relations inherit the smaller set. **Reclassified upward in part: `authenticated` held TRUNCATE on ~100 tables and RLS does not apply to TRUNCATE** — no path to it today (PostgREST does not expose it, authenticated has no CREATE) but it is a privilege no policy could ever constrain. Role-tested: anon still reads sports/zip_regions/tournaments; anon refused on invite_codes/error_logs/courts; members read courts/events/profiles_public and call policy predicates unchanged; member TRUNCATE on profiles refused. Boot sentinel `grant_hygiene_intact()` fails a deploy if any of it returns. |
| KCDX-017 | P1 | D1 | Authentication / MFA / CAPTCHA | Mandatory MFA and CAPTCHA abuse gates fail open | SECDB-013 / S2-SEC-004,S2-SEC-011 | PARTIALLY FIXED — TOTP lockout done; CAPTCHA/attestation open | **The Supabase MFA verification-attempt hook is gated to Team/Enterprise, not Pro** — established Aug 5 when Gabriel tried to enable it on Pro and could not. It is not the path and will not be. The equivalent policy already runs at the app layer: `verifyTotpAction` in `app/auth/actions.ts` fronts EVERY 6-digit code (login challenge and enrollment activation alike — the browser never calls `mfa.verify` directly), applying a per-IP strict throttle plus the 0055 policy of 5 wrong codes in 15 min → 15-min lock, against the `mfa_failed_verification_attempts` table already in production via `lib/mfa-lockout.ts`. That is a stronger position than the hook: it also covers enrollment, and it is testable in CI rather than living in a dashboard toggle. Still owed by this finding: fail-closed CAPTCHA/bot-protection classification, and deployment attestation for the Auth settings that DO live in the dashboard. |
| KCDX-018 | P1 | D1 | Testing / RLS assurance | The database safety test asserts neither private reads nor the real client role | SECDB-014 / S2-SEC-007 | **FIXED** — Tier-1 complete; Tier-2 still owed | Root cause confirmed: the old script sets `request.jwt.claims` but never `SET ROLE`, so it runs as the owner — RLS bypassed, grants unenforced. Its 'IDOR probe' proved nothing and printed PASSED. `supabase/tests/rls_negative_suite.sql` replaces it: 25 checks that switch to real `anon`/`authenticated`/`service_role` before probing, asserting BOTH directions (denials and permissions) across profile PII, queue presence, operator credential, tournament self-service writes, video containment incl. service_role, TRUNCATE, and the anon read surface. Wired into `supabase/harness/replay.sh`, so it runs on every replay in CI. **Verified by negative control** — re-granting `select on profiles`, re-granting `select on courts to anon`, and dropping `posts_remoderate_on_edit` each produced the expected failure. The third control exposed a vacuous fixture in the new suite itself (it approved a post as owner, which `guard_moderation_update` silently reverts, so the assertion passed for the wrong reason); the fixture now approves as service_role and asserts its own preconditions. Old file kept for its catalog-only block 1, with a header stating plainly why its block 3 cannot be trusted. **Still owed:** Tier-2 — PostgREST, Storage and Realtime have their own authorization layers and need a disposable Supabase project. |
| KCDX-019 | P1 | D1 | Outbound network / SSRF | Google short-link redirect walking creates a constrained blind SSRF primitive | SECDB-016 / — | **FIXED — both layers, plus a second hole found later** | (1) `isPermittedMapsHop()` before every fetch in the walk: allowlist of short-link + Google Maps/consent hosts; refuses non-https, userinfo, non-443 ports, IP literals and decimal/octal notations. (2) Egress control at the DNS lookup the socket uses (`lib/egress.ts`), closing DNS rebinding — Vercel's egress policy features are Enterprise-only, so this is enforced in-process. (3) **Second hole found during the KCDX-056 pass and closed:** the same file had a 'last resort' `fetch(raw, { redirect: 'follow' })` twenty lines below the fixed loop, which let the RUNTIME walk the whole chain unvalidated. Removed; the manual walk already covers the redirect shapes and an unresolved link falls back to geocoding the venue text. 22 tests. **Residual:** a future bare `fetch` still bypasses `safeGet` — the deadline tripwire added for KCDX-056 now catches absolute-URL fetches, which partially covers this. |
| KCDX-020 | P1 | F-Search | Search / Document coverage | Search documents omit key attributes and published businesses | SSF-SRCH-001 / — | **FIXED** — awaiting 0216 paste | Events/teams/tournaments/classes indexed titles and names ONLY, so search worked only if the member already knew what something was called. 0216 widens each document to the fields already rendered on that entity's public surface (events: +description +sport; teams: +city +neighbourhood +sport; tournaments and classes: +description +venue +sport), with the privacy review recorded in the migration — excluded: anything from `profile_private`, member ZIPs, provider `bio`, unpublished rows. **Published businesses had no search document and no branch at all**; both added, gated on `published AND status='active'`. Verified: a word appearing only in an event description now matches; an unpublished business correctly does not. |
| KCDX-021 | P1 | F-Search | Search / Providers | Professional name queries do not constrain provider retrieval | SSF-SRCH-002 / — | **FIXED** — awaiting 0216 paste | `class_providers.search_tsv` indexed `headline` and `roles`; the provider's NAME lives on `profiles.display_name` and a generated column cannot reach another table — so a coach was findable by name only if their name happened to appear in their own headline. 0216 adds `public_name`, maintained by triggers on BOTH sides (provider row written; profile renamed) and backfilled, then indexed alongside headline, area and roles. Denormalisation with a maintained invariant rather than a cache with a TTL. Verified: searching a coach's forename finds them, and renaming the profile makes the new surname match. Provider branch also now applies `is_discoverable_player` (0215) so a suspended or blocking provider is not surfaced. |
| KCDX-022 | P1 | F-Search | Search / Recurring events | Recurring events disappear because search filters the expired parent timestamp | SSF-SRCH-003 / — | **FIXED** — awaiting 0214 paste | The event branch filtered `events.starts_at`, the PARENT row of a series, so a weekly game dropped out of search a day after the series date while its occurrences kept happening and the event page kept rendering them. 0214 matches on the next occurrence that is actually going ahead (not skipped/cancelled/closed) via a lateral join to `event_occurrences`, and displays that date rather than the series creation date. Verified: a series with a parent dated 6 months ago was absent, is now found showing the next occurrence date; cancelling or skipping that occurrence correctly removes it again. |
| KCDX-023 | P1 | F-Search | Search / Privacy parity | Deterministic and AI search apply incompatible privacy, status, and block rules | SSF-SRCH-004 / — | **FIXED** — awaiting 0215 paste | Three surfaces disagreed: `global_search`'s player branch filtered NOTHING but text (suspended members and people who had blocked the caller were returned, with the app removing them after the branch limit AND the global cap, shrinking results); the AI path filtered `open_to_invites`+sport only; the app filtered a third set. 0215 centralises the question the audit names — **discoverability, distinct from RLS readability**: `is_discoverable_player()` (active, not self, not blocked either way), a set-based `discoverable_players()` for candidate lists, and `is_discoverable_tournament()` (not cancelled/suspended + visible). Applied in the player, tournament and provider branches BEFORE their limits, and in the AI player tool as the CALLER (not the privileged client) so 'may THIS member find them' is evaluated correctly. Verified with a non-zero baseline: suspend → gone, block → gone in both directions, unblock → restored. |
| KCDX-024 | P1 | F-Search | AI search / Prompt injection | Broad AI fallback exposes untrusted UGC to ranking and summary instructions | SSF-SRCH-005 / S2-SEC-014 | **MITIGATED** — defence in depth; no complete defence claimed | Tool results (member-authored titles/descriptions/names) were JSON-stringified into a USER turn, indistinguishable from the member's query. Three layers added: (1) `wrapUntrusted` delimits results in `<klimr_tool_result>`, labels them as data, and repeats the reminder AFTER the block, where redirection is most effective; (2) `neutralizeUntrusted` breaks the STRUCTURE injection relies on — role labels, turn tags, `[INST]`, code fences, heading markers, whitespace floods — rather than blocklisting phrases, with a test asserting content stays readable; (3) `validateSummary` strips markup, bounds length, and DROPS any summary containing a URL, since every legitimate link is hydrated from the server bank by id. System prompt now states the tool-result boundary explicitly. 6 tests. **Blast radius bounded by RLS, not by this:** tools can only return what the member may already see, so a successful injection can mislead about ORDER or PROSE, not exfiltrate. Recorded as mitigation, not a fix — prompt injection has no complete defence. |
| KCDX-025 | P1 | F-Search | Search / Result composition | Search kind filtering occurs after the global result cap | SSF-SRCH-006 / — | **FIXED** — awaiting 0214 paste | Each branch had a fixed limit, the union was capped globally, and `app/search/actions.ts` filtered by inferred kind afterwards — so a court search competed with players, teams and tournaments for the cap first and was narrowed second, and could return two courts where six existed. 0214 takes a validated `p_kinds text[]`: unrequested branches are skipped BEFORE ranking, and per-branch limits scale (up to 4x) as fewer kinds are requested. `p_kinds => null` preserves prior behaviour for any caller that has not opted in. Verified: all-kinds, event-only, court-only, unknown-kind and null all behave correctly. **Two near-misses recorded:** a default arg ADDS a signature rather than replacing it (ambiguous overload → runtime failure for every existing caller; old signature now dropped explicitly), and the second return column is `id` not `ref` — renaming it would have compiled and broken the search page. |
| KCDX-026 | P1 | F-Social | Discovery / Location privacy | Discovery surfaces expose neighborhood despite the city/state rule | SSF-SOC-001 / — | **FIXED** — awaiting 0206 paste | `publicLocationLabel` already preferred `city`, but its parameter TYPE admitted `neighborhood`, so every caller fetched the finer location to satisfy the signature and it travelled through the discovery RPCs, the search subtitle and the PYMK rail regardless of display. Type no longer admits it; search hydration moved to `profiles_public`. **Also fixed a live regression from 0191 found during this work:** four call sites still read private columns with the caller's session — `/profile/[id]` selected date_of_birth/birth_year/home_zip/neighborhood/account_status for another member, so with 0191 applied EVERY member profile page returned 404. All four moved to `profiles_public`. 0206 recreates the projection with a derived `age` (sources withheld) and `security_invoker = false`, which is required for a projection to publish a value computed from columns the reader cannot see. The 0191 guardrail was file-level (skipped any file containing an admin client anywhere) and is now statement-level. |
| KCDX-027 | P1 | F-Social | Social graph / Concurrency | Simultaneous opposite-direction connection requests do not auto-accept | SSF-SOC-002 / — | **FIXED** — awaiting 0208 paste | The `for update` locked the pair row only when one already existed — the exact case this race is not. Two simultaneous opposite-direction requests both found no row, both reached the insert, and the loser's `unique_violation` handler returned `already_requested` without re-reading: pair left `pending` instead of `accepted`, and the second person told they had asked when they had not. Now takes `pg_advisory_xact_lock` on the canonical ordered pair BEFORE the lookup, so A→B and B→A serialize with nothing in the table; the unique-violation branch re-reads and runs the reverse-accept transition. **Proven with two genuinely concurrent psql sessions:** B `requested`, A `accepted`, final state `accepted`. |
| KCDX-028 | P1 | F-Social | Blocking / Relationship integrity | Block and unblock do not enforce one complete relationship invariant | SSF-SOC-003 / — | **FIXED** — awaiting 0208 + 0209 paste | 0208: block/unblock are commands sharing a pair advisory lock, with the edge-removal matrix written once (graph, follows, decline memo, PYMK); unblock replaces raw `blocks` DELETEs in two server actions and restores discoverability but deliberately NOT the relationship. 0209 closes what a blocked pair can still SEE: `is_blocked_pair()` (present since 0099 but granted to service_role only — the reason four policies inline their own copy) is granted to `authenticated` and now backs the `posts` policy, `post_visible()` and a NEW block test on `feed_items`, whose read policy had none. `notifications.actor_id` added with a BEFORE INSERT trigger dropping deliveries between a blocked pair, plus a both-directions purge on block. 20 person-to-person call sites wired with actorId; 41 system/admin notices remain, pinned by a ratchet test. Verified with real roles and a NON-ZERO baseline: A sees B's post and 2 feed items → block → 0/0 both directions, unrelated items unaffected → unblock → restored. `get_ranked_feed` confirmed INVOKER, so the main feed was already covered. |
| KCDX-029 | P1 | F-Social | Recommendations / PYMK | PYMK invalidation and dismissal are incomplete | SSF-SOC-004 / — | **FIXED** — awaiting 0211 paste | Dismissal was React state (`setRows(filter)`), so a refresh restored the person permanently. Now `pymk_dismissals` with a 90-day expiry — 'not right now' and 'never' are different answers and one tap should not be permanent — written by `pymk_dismiss()`, which also clears the cached payload. For the 24h cache served verbatim: `pymk_valid_targets()` revalidates every cached identity in SQL before display (active, not blocked either way, not dismissed, no existing friendship in any state), so partial invalidation stops being a correctness dependency. Added the missing deactivation invalidation as a trigger. Rail persists via a new `dismissSuggestion` action and rolls the row back if the write fails. Verified against a NON-ZERO baseline: [B,C] → dismiss B → [C] and the row persists → C deactivates → [B] → C reconnected → [B] → block B → none. |
| KCDX-030 | P1 | F-Social | Network / Scalability | Network and played-together reads truncate or scale with lifetime history | SSF-SOC-005 / — | **PARTIALLY FIXED** — lifetime scan removed; pagination + pool ordering measured, not rewritten | The played-together map selected EVERY `match_participants` row for the viewer, chunked the ids 400 at a time, pulled every participant of every one of those matches into Node, and counted in JS — O(lifetime) per render of `/network`, against the standing scalability rule. `played_together_counts()` (0213) does it in one indexed join scoped to `auth.uid()`, with covering indexes; verified correct (B→3, C→1) and caller-scoped (a third member sees only their own). **Side effect worth noting: the page no longer needs elevated rights at all**, so it comes off the admin grandfather list (87 → 86). **Not done, deliberately:** cursor-paginated network RPCs, and the PYMK candidate pools' missing ORDER BY under `limit 400`/`200` — changing candidate selection changes what every member sees, so `pymk_pool_saturation()` measures how many members are actually at a cap first. |
| KCDX-031 | P1 | F-Social | Social graph / Durability | Social graph commit and notification delivery are not durable together | SSF-SOC-006 / — | **FIXED** — awaiting 0212 paste | Graph mutation and notification were separate operations, so a reclaimed invocation left the edge committed and the person never told, with no record anything was owed. 0212 adds `social_outbox`: an AFTER trigger on `friendships` writes the delivery intent in the SAME transaction as the edge; `deliver_social_outbox()` claims batches with FOR UPDATE SKIP LOCKED, delivers, retries on failure, and cannot duplicate (unique dedupe key per edge+kind); scheduled every minute via pg_cron; `social_outbox_stuck()` reports committed-but-undelivered events. Verified: request → edge pending + outbox row, deliver → notification with actor set, rerun → 0 delivered and no duplicate, accept → second event delivered, stuck = 0. **Live bug found and fixed alongside: `notifications_kind_check` allowed 8 kinds while the app sends 13** — friend_request, friend_accept, tournament, waitlist_offer, waitlist_expired all violated it across 11 call sites, and `createNotification` ignored the returned error (supabase-js does not throw), so NONE of those notifications had ever been delivered. Constraint widened; the insert error is now logged. |
| KCDX-032 | P1 | F-Social | Privacy / Policy matrix | Member relationship and privacy controls lack a complete policy matrix | SSF-SOC-008 / — | **PARTIAL — reopened by re-audit (KRA-008/009)** | 0233/0234 applied to production 2026-08-10. The written policy exists and the ladder collapses 15 cells into 5 audience settings — that half is done. **But the 2026-08-10 Codex re-audit found the predicates (`may_act_on`, `may_see_connections`, `comment_visible_to`, `is_muted_by`, `is_restricted_by`) are DEFINED and never CALLED** — verified: zero enforcement call sites outside the migration definitions and string-presence tests. The comment read policy still lacks mute/restrict enforcement (0006 policy unchanged), and the arbitrary-pair helpers are granted to `authenticated`, making them relationship oracles. Tracked forward as KRA-008 (enforce) and KRA-009 (de-oracle). The original finding — "no written policy" — is closed; the enforcement of that policy is the open work. |
| KCDX-033 | P1 | F-Feed | Feed / Scope selector | The Feed scope selector is nonfunctional and circle filtering drops follows | SSF-FEED-002 / — | **FIXED** | `feed-controls.tsx` writes `?scope=`; `feed/page.tsx` read `?lane=`. They never met — clicking 'Your circle' navigated to a parameter the page ignored, defaulted to nearby, and the feed did not change. **The primary control on the primary surface did nothing**, and nothing failed, which is why it survived. One name now, `lane` still accepted for existing links. Underneath it, the page re-filtered ranked results against a FRIENDS-ONLY set while `get_ranked_feed` had already scoped to circle using friends AND follows — so 'Your circle' would have dropped everyone you follow but are not connected to. The duplicate filter is deleted rather than corrected; a second definition would drift again. |
| KCDX-034 | P1 | F-Feed | Feed / Safety controls | The active Feed lacks report, deletion, thread, and moderator media controls | SSF-FEED-003 / — | **PARTIALLY FIXED** — reporting path built; moderation workflow owed | The active card had NO report control, no delete, no reply; `deleteOwnPost` existed and nothing reached it. **A member seeing harmful content in the Feed had no way to tell us — none.** Every other Feed safety control is automated detection; reporting is the one that catches what automation misses. 0225 adds `post_reports` + `report_post()`: one open report per person per post, generous per-reporter rate limit, and a SNAPSHOT of the body plus preservation of the media as a `safety_incidents` row — because the author can delete the content the moment they suspect a report, and a report that dies with its subject stops working exactly when someone is trying to escape it. `post_id` nullable ON DELETE SET NULL so the report outlives the post (a `not null` + `set null` contradiction in my first draft made deleting a reported post fail outright — caught by test). Report control added to the active card, plain and always visible rather than behind a hover menu. **Still owed:** owner edit/delete and one-level reply on the card, comment deletion, block/mute from the card, report-queue SLA and audit retention, safe moderator media preview. |
| KCDX-035 | P1 | F-Feed | Feed / Media lifecycle | Failed Feed creation and ordinary deletion orphan media | SSF-FEED-004 / — | **FIXED** — awaiting 0224 paste | The composer uploads before creating the post, so every abandoned composition left bytes behind; `deleteOwnPost` removed the row and not the object; only moderation REJECTION cleaned up. An orphaned object is content a member BELIEVES they deleted — gone from every surface, still reachable by anyone holding a signed URL. 0224 adds a `posts_delete_media` trigger (object goes with the post) and `purge_orphan_feed_media()` on a 24h grace period — long enough that a post being composed right now is never collected, which is the difference between a garbage collector and a race with the person typing. Both skip objects preserved as safety evidence, which outlive their post by design. Verified: abandoned removed; referenced, in-flight and evidence all kept, including when the evidence's own post is deleted. |
| KCDX-036 | P1 | F-Feed | Feed / Pipelines and scale | Two Feed pipelines and row-by-row engagement work will not scale cleanly | SSF-FEED-005 / — | **PARTIALLY FIXED** — engagement set-wise and measured; one-pipeline + keyset owed | The ranking scored each candidate with two CORRELATED scalar subqueries over up to 500 candidates (a thousand point lookups per feed load, per viewer), and the page then ran its own aggregate pass over the same posts. 0229 counts once and returns `likes`, `comments` and `viewer_liked` WITH the ranking. **Measured rather than asserted — my first comment claimed a hash aggregate and the plan showed a nested loop (loops=300), so the migration now carries numbers: ~520ms → ~275ms** over 5 runs each on 300 posts / 2,400 likes / 200 comments. **Still owed:** one canonical Feed pipeline (the page still loads and ranks legacy data separately), the system digest as an explicit projection, type filtering pushed into SQL before the limits (same shape as KCDX-025), and keyset pagination. |
| KCDX-037 | P1 | F-Feed | Feed / Composition and accessibility | Feed composition, comments, and accessibility are below production UGC baseline | SSF-FEED-006 / — | **PARTIALLY FIXED** — composer fixed; cursor paging and media descriptions owed | The composer was a single-line `<input>` where Enter SUBMITTED — a member writing anything with a line break could not, because Enter posted the half-written thought. On a surface whose purpose is people writing things, that is a content-quality problem disguised as a keyboard shortcut, and it fails silently because the result looks like a post the person chose to make. Now a textarea: Enter breaks the line, ⌘/Ctrl+Enter submits, the button stays primary, and the shortcut is announced via `aria-describedby`. **Still owed:** cursor pagination for comments (currently loads oldest 200), draft/upload recovery, meaningful media descriptions instead of body/default alt, caption/transcript creation, and preserving one-level threads. |
| KCDX-038 | P1 | F-Feed | Feed / Reposts and deep links | Repost/share machinery contradicts the active product and Feed deep links are unreliable | SSF-FEED-007 / — | **MOSTLY FIXED** — chat attachments still owed | **Deep links:** the card copies `/feed?post=<id>` and the page never read `post`, so every share link ever sent landed on the generic Feed — and since the Feed is ranked and personalised the recipient often could not find the post at all. Nothing errored, which is why it survived. `resolve_feed_post()` (0228) fetches by id and distinguishes deleted / not-visible / pending-review, with a blocked pair deliberately collapsed into the deleted answer because any distinguishable response reveals the block; the page renders a precise unavailable state. **Reposts:** the machinery existed in 3 places (0133 schema+trigger+index, the toggle action) and the product denied it in a 4th (the ranking EXCLUDES reposts and indexes on that exclusion; the card tells members reposts do not exist) — so a repost was a real row no feed would ever show. The action now refuses; the schema stays because production rows may exist, the implementation is deleted because a dead function that works invites a caller. **Still owed:** typed chat post attachments. |
| KCDX-039 | P1 | D4 | Routing / Middleware semantics | Human-session middleware redirects cron, Courtside fleet, and public telemetry routes | ROOT-RTE-001 / S2-REL-001 | **FIXED** | `PUBLIC_PATHS` was written for humans and omitted six machine surfaces: both crons, courtside register/heartbeat, csp-report, rum — each got a 307 to an HTML login page. **Both scheduled jobs have therefore never executed**, and CSP/RUM telemetry has never landed. The handlers were never the problem: `finalize-tournaments` already verifies CRON_SECRET fail-closed and `courtside/register` already rate-limits strictly — the request never arrived, and a 307 to HTML reads as a 200 to any client that follows redirects, so Vercel's scheduler reported healthy crons. **Collision with our own remediation:** 0192 makes the Courtside device token the only route to recording a match result, so shipping KCDX-007 without this would have bricked every kiosk. `lib/route-manifest.ts` now declares a class per route (public/human/aal2/machine); machine routes pass through, and no `/api` path is ever redirected — an unlisted one fails closed with 401 JSON. 7 tests reading routes off disk and cron paths out of vercel.json. |
| KCDX-040 | P1 | E | Queue / Placement uniqueness | Queue placement lacks session-wide uniqueness and complete idempotency | RP-009 / — | **FIXED** — awaiting 0219 paste | 0176 serialized placements per COURT and logged an idempotency key. But 'one team per person per session' is SESSION-scoped and the check lived in TypeScript before the RPC — two taps on two courts of one session took two different locks, both read a clean session, and both placed (member queued twice, scored twice, undetectable since the only unique index is per team). `place_on_team` now takes a session+user advisory lock FIRST then the court — consistent ordering, so concurrent placements serialize without deadlocking — and enforces the rule after the lock. **Proven with two genuinely concurrent transactions on different courts:** one succeeds, the other raises `already_in_session`, player on exactly one team, `queue_placement_intact()` true. **The idempotency key was built and never passed:** both call sites now supply derived keys (`join:court:user`, `approve:request`) so a double-tap or retry places once. |
| KCDX-041 | P1 | E | Queue / Match and ranking transitions | Queue match and ranking transitions are non-transactional and retry-unsafe | RP-010 / — | **FIXED** — awaiting 0218 paste | Finishing a match was 8 separate writes. The leading CAS on `status='live'` looked protective and was the opposite: if the invocation died after it, the match was FINAL and the other 7 never ran — and since the CAS then fails, NO retry could repair it (winner never advances, nobody gets points, counters never move, nothing records the debt). Counters were also read-modify-write, so two courts finishing at once with a shared player lost an increment. `queue_finish_match()` does all of it in one transaction with the match row locked; counters increment atomically; the ledger keeps its (match_id,user_id) natural key; idempotency comes from STATE, so a second call reports success and awards nothing. Verified: finish → final/winner holds at 1 win/loser retired/ledger 12-4/counters 1 played each/points recomputed; replay → `already_final`, nothing changed; invalid winner → refused. Dead `awardQueueMatchPoints` deleted. Constant-drift test pins the SQL point values to `lib/ranking.ts`. |
| KCDX-042 | P1 | E | Queue / Read safety and polling | Queue reads can destructively wipe state while polling hides and amplifies failures | RP-011 / — | **FIXED** — awaiting 0217 paste | `loadSessionState` (polled every 3s per viewer, plus every SSR) called `retireSessionIfStale`, which past 12h idle called `wipeSession` — destructive delete of play state, courts and settings, plus flipping the parent event's queue toggle. N viewers = N unsynchronised wipe attempts with no lock; idempotent only by luck. Read is now side-effect-free; retirement belongs solely to `end_stale_court_sessions` (0207, hourly, transactional, history preserved). 0217 moves the **one-switch rule** into `end_court_session` so the toggle also flips on Force end, where it had been missing. Court configuration deliberately not wiped by automatic expiry. **Polling hardened:** single-flight with abort, exponential backoff with jitter, paused while the tab is hidden, immediate refetch on return, and a visible stale banner after two consecutive misses — previously every failure was swallowed and a dead network showed a confident stale queue. |
| KCDX-043 | P1 | E | Events / Capacity | Event RSVP and approval capacity checks are subject to TOCTOU races | RP-012 / — | **FIXED** — awaiting 0220 paste | Two different failures. RSVP counted the cycle's `going` rows then upserted with nothing holding between — two people on the last seat both got in. **Approval was not a race at all: `approveMember` set `going` with NO capacity check whatsoever**, so an organiser could admit forty people to a twelve-person event; the capacity column has existed since 0017 and that path never read it. `event_admit()` locks the event, counts under the lock, and writes; approvals go through the same command, so a seat is a seat. Idempotent (existing `going` short-circuits before the count). Cycle boundary stays in `lib/` and is passed in — the function is service_role-only precisely because a caller who chooses the boundary chooses the count. Verified: 4 RSVPs into 2 seats → 2 going + 2 `full`; two concurrent transactions on the last seat → exactly one wins; approving into a full event → refused. **Deliberately no `waitlisted` status:** the constraint permits going/pending only, and a status no surface renders would strand people invisibly — an event waitlist is a feature, not a status value. |
| KCDX-044 | P1 | E | Open play / Waitlist state machine | Open-play waitlist reservations are not one atomic durable state machine | RP-008 / — | **MOSTLY FIXED** — commands built; job/outbox still owed | Confirming was 5 unguarded steps (read offer, read match, COUNT participants, insert, mark joined) — and the race is the ORDINARY case, since promotion issues several offers at once so the fastest responder wins. The last two steps were also unrelated: a participant could land while the offer still read `offered`, letting the sweep expire an offer already taken. Promotion computed free slots from two independent counts and ignored every write error (`if (error) continue`). `match_confirm_offer()` and `match_promote_waitlist()` are now single locked transactions on the match row, so they serialize against each other; promotion claims the FIFO head with FOR UPDATE SKIP LOCKED and renumbers the line in the same transaction (renumbering was previously a separate round trip, so the line a member saw did not match the one that existed). Notifications moved AFTER commit — they were being sent from inside the loop, promising a state that could still roll back. Verified: 2 slots + 3 waitlisted → exactly 1 offer + renumber; repeat → 0; two concurrent confirms on the last slot → one ok, one `full`, invariant true. **Still owed:** the deduplicated job/outbox written with state (jobs.ts names kinds the worker route does not handle). |
| KCDX-045 | P1 | E | Tournaments / Capacity and reconciliation | Tournament capacity and structure reconciliation are application races | RP-006 / — | **PARTIALLY FIXED** — admission locked; reconciliation still application-side | The registration capacity race was closed by 0193's `tournament_register`, which locks the tournament row and counts under the lock (verified: second entrant at capacity 1 → waitlisted). **Still owed:** the demotion/promotion/renumber/schedule-reset reconciliation loop still runs as application code, there is no immutable structure revision for schedules and brackets to bind to, and notifications are not emitted from a post-commit outbox. Same dependency as KCDX-046's graph generation — both need the revision model first. |
| KCDX-046 | P1 | E | Tournaments / Bracket integrity | Tournament bracket, result, rollback, and schedule graph edits can partially commit | RP-007 / — | **PARTIALLY FIXED** — score/advance/rollback transactional; graph generation not revisioned | Scoring had NO compare-and-swap and advanced the winner in a SEPARATE statement — two staff entering results for the same match (sheet vs court, which is how a desk runs) both wrote and each advanced a different entrant, so the bracket could carry the loser forward; a failure between left the match completed with nobody advanced. Clearing was worse: it nulled the downstream slot UNCONDITIONALLY, so correcting a QF after the SF was played removed a player from a semi they had already won, leaving a completed match with an empty side and no way to reconstruct the bracket. 0222 makes both single locked transactions, adds optimistic concurrency (`changed_since_view`), and REFUSES a clear when the downstream match has been played — a correction that invalidates played matches is an adjudication. `bracket_graph_intact()` detects the completed-match-with-empty-side signature. Verified on a two-round fixture incl. non-staff refusal. **Outstanding:** graph generation (draw, round linking, bye advancement) still runs as application loops and needs the immutable structure revision; recorded rather than half-built. |
| KCDX-047 | P1 | E | Tournaments / Concurrency | Tournament JSON lost-update remediation is incomplete | RP-013 / — | **FIXED** — awaiting 0205 paste | 0179 built the locking merge and wired it into 2 sites; 13 read-spread-write siblings remained, each reading the whole `format_config`, editing one key in JS, and writing it back — so concurrent staff edits silently erased each other. **Root cause of the incompleteness: `merge_format_config` was granted to `service_role` only**, so cookie-session server actions could not call it and the wrong path was the only available one. 0205 moves the staff check inside the function (`is_privileged_writer() or is_tournament_staff()`) and grants it to `authenticated`. All 13 sites converted to a single `patchConfig()` helper; guardrail test fails the build on any `.update({ format_config … })`. Verified: a staff patch of `prizes` leaves `sponsors` intact; a non-staff caller gets `not_tournament_staff`. |
| KCDX-048 | P1 | D3 | Teams / Ownership integrity | Team ownership can split across membership role and teams.created_by | RP-014 / — | **FIXED** — awaiting 0202 paste | Ownership lives in `team_members.role` and `teams.created_by`; transfer moved them with three unchecked statements and owner-departure with four, so every gap was a state with two owners, none, or a `created_by` disagreeing with the roster. `team_transfer_ownership()` and `team_leave()` are single locked transactions with `get diagnostics row_count` after each write — a transfer touching the wrong number of rows raises rather than half-completing. Role-tested: non-owner → not_owner; non-member target → not_a_member; valid transfer swaps both rows and `created_by`; owner leaving promotes the longest-standing member atomically. `team_ownership_intact()` asserts exactly one owner per roster and agreement with `created_by`, and was verified to return FALSE when forced into a genuine two-owner state and when `created_by` was made to disagree. |
| KCDX-049 | P1 | D3 | Marketplace / Concurrency | Marketplace offer acceptance and thread identity are not listing-wide invariants | RP-015 / — | **FIXED** — awaiting 0204 paste | The accept was a TypeScript `status === 'open'` check followed by an UNCONDITIONAL update keyed on the offer id, then a separate listing update, with competitors never touched — two tabs both accepted, and other buyers kept waiting on a sold item. `marketplace_offer_respond()` locks the LISTING (the contended resource), re-reads the offer under it, compare-and-swaps on `status='open'` with a row-count check (`race_lost`), expires all competing open offers, and moves the listing to pending — one transaction. Partial unique index `listing_offers_one_accepted` makes a second acceptance unrepresentable; existing duplicates repaired to the earliest decision. Role-tested: two buyers offer, seller accepts one → second returns `not_open`, competitor auto-expired, listing `pending`. **Live bug found and fixed in the same migration:** two conflicting status CHECK constraints on `marketplace_listings` made `'active'` the only satisfiable value, so sold/pending/removed had never been possible; the stale one is dropped. |
| KCDX-050 | P1 | D3 | Rankings / Data integrity | Ranking recomputation can overwrite canonical points after partial reads | RP-016 / — | **FIXED** — awaiting 0200 paste | Root cause: `lib/points.ts` consumed both ledger reads as `result.data ?? []`, and a supabase-js read that ERRORS returns `data: null` — so a transient failure became an empty ledger and the resulting total was written as canonical `player_sports.points`. 0200 moves the whole computation into `recompute_player_points()`: best-8-of-52-weeks across both ledgers, read and written in ONE statement, so the partial-read case is removed rather than handled and the two-read window is closed. Wrapper now throws instead of writing. Verified: 660 across mixed ledgers, aged-out rows excluded, matches_played/wins untouched, member self-edit still refused, function is service_role-only. **Found by the test:** the first version returned 660 and stored 0 — `guard_player_stats` reverts unless `current_user = 'service_role'`, and inside a SECURITY DEFINER function current_user is the definer. Fixed with a transaction-local writer flag (a second role check would have hit the same wall). Constant-drift assertion added so the SQL's 52/8 cannot diverge from `lib/ranking.ts`. |
| KCDX-051 | P1 | G | Testing / Behavioral contracts | The green test suite omits the highest-risk behavioral contracts | ROOT-TEST-001 / — | **PARTIALLY FIXED** — database contracts covered; browser contracts owed | Now running on every replay, in CI: **migration replay** from zero AND from the production baseline; **role-negative suite** (26 checks as real anon/authenticated/service_role, both directions asserted); **concurrency suite** (5 races, each two psql sessions sleeping to the same instant — mutual connection requests, one person two courts, last event seat, two offers one slot, tournament capacity); **readiness gate** (16 boundary checks, discovered not listed, with a count assertion). The concurrency proofs previously existed only in transient shell sessions — the fixes were in migrations, the evidence nowhere. **Verified by breaking one:** reverting `place_on_team` to its pre-0219 form makes the suite report `got '2', want '1'`. The queue test that 'explicitly expected the operator code to remain public' was reversed in the 0192 batch. **Still owed:** 10-15 Playwright journeys, axe/keyboard checks and performance budgets — these need a running app and a browser, a different harness than anything here. |
| KCDX-052 | P1 | G | Release engineering / Schema contract | Release engineering can ship an application against the wrong database contract | ROOT-REL-001 / — | **MOSTLY FIXED** — artifact promotion still owed | 0190 already added the schema manifest (objects + service_role EXECUTE) and moved the boot sentinel out of Vercel-only; the `schema-replay` CI job applies every migration from zero and from the production baseline. This batch closes the rest: **sixteen `%_intact()` boundary checks existed and only SIX were wired** — each batch added its own probe and its own wiring, which guarantees the newest check is the unwired one. `klimr_readiness()` discovers them by name; `klimr_ready()` also asserts a COUNT because a dropped check vanishes rather than failing (the gate had the very bug it exists to catch). Wired into the boot sentinel AND into CI after the replay, so a migration that opens a boundary fails the build rather than the deploy. **Node pinned to one number** across `engines`, `.nvmrc`, CI and README — previously three different answers — with a test asserting they agree. **Still owed:** promoting one immutable migration artifact and a post-deploy smoke run against production. |
| KCDX-053 | P1 | G | Disaster recovery / Backups | Recovery objectives and backup coverage are unproven | RP-019 / S2-REL-002 | **PARTIALLY FIXED** — verification tooling built; the DRILL and the Storage copy remain owed | Supabase Pro gives daily DB backups, 7-day retention, no PITR. **Storage has no backup of any kind** — a DB restore returns every row pointing at bytes that no longer exist. 0226 builds what makes a Storage backup VERIFIABLE rather than the backup itself (a migration cannot copy bytes, and pretending otherwise would repeat the error being corrected): `storage_manifest_take()` fingerprints every object, `storage_manifest_verify()` names what is missing or altered after a restore, `storage_manifest_summary()` gives the drill row. The distinction that matters is `missing` vs `content_differs` — 'the file came back' and 'a file with that name came back' are different outcomes, and a partial Storage restore otherwise looks exactly like a complete one. Verified by simulation. RESILIENCE.md targets relabelled **UNVALIDATED** with a test pinning `drill-run=never` until one is recorded, and the RPO row now states it is database-only. **Still owed:** the object copy itself, the throwaway-project drill with measured RPO/RTO, and PITR/off-provider copies. |
| KCDX-054 | P1 | D1 | Privileged access / Auditability | Service-role usage remains broad and its audit layer is partial and non-durable | ROOT-PRIV-001 / S2-SEC-009 | PARTIALLY FIXED — durability done; breadth still owed | **Durability (done):** the audit write moved from `void (async () => …)()` — a floating promise a serverless invocation can outlive — to `after()`, which keeps the invocation alive until the row lands; falls back to inline await outside request scope (cron/scripts). Empty catch replaced with a loud log carrying the command id. 0197 adds `command_id` + `outcome` (started/ok/error) with partial indexes, including one for the incident query 'which privileged operations started and never finished'. New `withPrivileged()` wrapper records outcome, not merely that a client was handed out. **Breadth (owed):** 88 files still import the raw admin client; grandfather list checked and NOT shrinkable — every listed file still needs it for other calls, so it stays at 87 and no progress is claimed. The real fix is privileged mutations becoming narrow domain commands with the audit transacted inside them (0193's tournament commands are the pattern); that is per-domain work in each domain's batch. Two ratchets added: grandfather list may only shrink; audit write may not revert to a floating promise. |
| KCDX-055 | P1 | D4 | Support chat / Third-party data sharing | Support chat sends home ZIP to Anthropic and uses a fail-open cost limiter | ROOT-AI-001 / — | **FIXED** | `runLookup` selected `home_zip` and returned it to Anthropic as 'Home area' — the finest location Klimr holds, leaving for a support conversation with no use for it, while the tool's own description promised only name/status/sports/join-date. ZIP removed; city retained and now DECLARED in the tool description, so the contract matches the behaviour. The cost gate moved from `rateLimit` (returns true on any infrastructure error — a DB hiccup removed the ceiling in front of a paid vendor call) to the existing fail-closed `rateLimitStrict`. Vendor timeout already added under KCDX-056 (20s, 1 retry, `anthropic` breaker). |
| KCDX-056 | P1 | D1 | Reliability / External services | Several external calls lack timeouts, retry policy, and circuit breakers | ROOT-NET-001 / S2-SEC-012 | **FIXED** — breaker scope recorded as a limitation | `lib/external.ts` provides `callExternal` with a MANDATORY timeout (the type requires it), opt-in retry (idempotence is the caller's promise — Resend gets none, model calls get one), and a per-vendor circuit breaker with jittered backoff. Every previously-unguarded outbound call wrapped with a per-site budget and written reasoning: Turnstile 3s/1 (fails open — a hung vendor used to hold the request then open the gate anyway), CSAM scan 10s/0 (fails closed), safety alert 5s/2, Resend 8s/0, Anthropic 20s/1 and 8s/1, Places 6s/1, OpenAI moderation 8s/1. 6 tests covering timeout-aborts-signal, no-retry-by-default, retry-then-succeed, breaker-opens-and-fails-fast, breaker-recovers, and vendor-independence. Tripwire fails the build on any absolute-URL fetch without a deadline. **Limitation recorded, not glossed:** the breaker is in-process, so it protects a warm instance and is not a global limiter; a shared one needs a per-call store round trip and should wait for traffic to measure. |
| KCDX-057 | P1 | D4 | Data rights / Export completeness | The self-service account export omits most documented personal data | ROOT-DSAR-001 / — | **FIXED (partial-with-index, deliberately)** | Returned 5 things while DATA-GOVERNANCE enumerated 9 categories marked 'SPECIFIED, NOT BUILT' — the worst shape for a data-rights route, since the member believes they have their data and nobody is prompted to fulfil the rest. Now `format_version: 2` covering all nine: identity, social (friendships/follows/blocks/invites, both directions), content, play (RSVPs, tournament registrations, queue participation, rank snapshots), teams, commerce (listings, offers, professional-status requests), communications (thread metadata + notifications), safety (reports filed + actions taken about them, staff identity withheld), devices (scoped to sessions the member organizes, per the governance doc). Carries a `coverage` block naming every exclusion WITH a reason — E2EE chat bodies, media by path, other participants' contributions, staff identities, auth secrets — each routed to verified manual fulfilment. DATA-GOVERNANCE corrected and tied to the route by a doc-claims test. **Still owed:** media bundle job and delivery audit evidence. |
| KCDX-058 | P1 | D1 | Documentation / Governance / Operational truth | Security, data-governance, resilience, and README assurances contradict the source and runtime | ROOT-DOC-001 / S2-DOC-001 | **FIXED** — mechanism added, not just wording | Five false claims corrected against verified source: RESILIENCE.md claimed Storage was in the project backup (it is not — Postgres only; the claim that stops someone building the missing backup); SECURITY.md claimed zero `dangerouslySetInnerHTML` (there are 10, all fed sanitised or server-generated HTML — claim restated as 'no unsanitised sink'); README claimed keyless pages render (middleware constructs a Supabase client unconditionally → 500, proven at runtime by the audit); SECURITY.md claimed user media disabled (photo upload is live behind the fail-closed CSAM seam — VIDEO is what is disabled, per 0195); DATA-GOVERNANCE described profiles readers as 'RLS-scoped' (until 0191 every member read every column). **The mechanism is the deliverable:** countable claims are tagged `<!-- claim:name=value -->` and asserted against source in `tests/doc-claims.test.ts` (6 assertions), so adding an 11th sink or reviving a retracted sentence fails the build. SECURITY.md now carries owner + reconciliation date, enforced by test. |
| KCDX-059 | P2 | G | Storage / Legacy media | Legacy post-media remains publicly readable after the new Feed bucket became private | SSF-FEED-011 / — | **CONTAINED** — access closed; deletion needs a production inventory | Confirmed from the database: `post-media` was `public=true`, `anon` SELECT policy, NO size limit, NO MIME allowlist — while `feed-media` is private with both. **No application code references `post-media` at all**; post media is served from `feed-media` via `createSignedUrls`. So every object there was member content nothing renders and anyone holding a URL could fetch forever. 0210 sets public=false, adds 10 MiB + image allowlist, drops the anon policy, and adds `legacy_media_retired()` to the sentinel set. **Deletes nothing by design** — `post_media_inventory()` reports remaining objects and whether any post row still references each one, because a migration cannot know what production holds; closing access first and inventorying second means the exposure stops today and nothing is destroyed before anyone looks. |
| KCDX-060 | P2 | F-Search | Search / Responsiveness | Search request coordination and AI deadlines can starve the newest query | SSF-SRCH-007 / — | **FIXED** | The panel REFUSED a new query while an older one was in flight (`if (ai.state === 'loading') return`), so refining a search left the member watching a spinner for a question they had already replaced and then handed them the stale answer. Latest-token superseding added: a run that is no longer newest writes nothing, and `resetAi` supersedes anything in flight. AI budget cut from 25s/4 rounds to **8s/3 rounds** — 25s is a batch budget, and a member cannot distinguish a slow answer from a broken one while deterministic results are already on screen. Reducing rounds also exposed two always-true `round < 3` retry guards, now `< 2`. **Not done:** a true abortable endpoint (a server action cannot be cancelled from the client). |
| KCDX-061 | P2 | F-Search | Search / Telemetry | Search relevance evidence is too small and zero-rate telemetry is mathematically wrong | SSF-SRCH-008 / — | **PARTIALLY FIXED** — arithmetic corrected; relevance corpus still owed | The zero-rate divided zeros by `search_deterministic`, and the app recorded ONE metric per search via a ternary — so the denominator was HITS, not searches, despite the comment claiming both were recorded. The error scaled with the quantity measured: 2 zeros in 10 reported 25%, 5 in 10 reported 100%, and an all-miss period divided by zero and reported NOTHING — calmest exactly when search was most broken. Now one row per search plus a second on a miss; demonstrated 25% → 20% for the same ten searches. **Still owed:** the 120-query seeded relevance corpus with per-kind Recall@10, MRR/NDCG, privacy false positives, latency and AI groundedness. |
| KCDX-062 | P2 | F-Social | Network UI / Error handling | Network optimistic mutations swallow server rejection | SSF-SOC-007 / — | **FIXED** | `run()` caught and ignored every rejection with the comment 'a refresh reconciles if the server rejected' — nothing triggers that refresh, so a request the server refused (blocked, rate-limited, cooldown) kept rendering as 'Requested' and the reason was never shown. The form wrappers compounded it by returning `void`, discarding the result before the caller could see it. Wrappers now return `SocialActionResult` (a `<form action>` ignores return values, so those callers are unaffected); the browser snapshots the row before the optimistic patch and restores it on any non-success; the server's message is surfaced and announced via an aria-live region, because a visual-only revert reads to a screen reader as a value that changed for no reason. |
| KCDX-063 | P2 | F-Feed | Feed / Ranking copy | Feed ranking copy and type filtering do not match behavior | SSF-FEED-009 / — | **MOSTLY FIXED** | The label read 'NEWEST FIRST · ALWAYS' above a feed ranked on recency, sport affinity, graph closeness, engagement and an author-diversity penalty. Recency is the largest single term, which is presumably where the copy came from — but 'always' is a promise the ranking does not make, and a member seeing a 3-hour-old post above a 1-hour-old one is entitled to think the feed is broken. Now 'RANKED FOR YOU' with the actual signals named. **Still owed:** passing the type filter INTO the RPC before its candidate/result limits (same shape as KCDX-025, which is done for search). |
| KCDX-064 | P2 | G | Observability / Telemetry | Observability misses failures and several ingestion paths are empty | ROOT-OBS-001 / — | **MOSTLY FIXED** — canaries built; per-plane separation still owed | Several parts closed with earlier work: Queue polling no longer swallows fetch errors (0217), the middleware no longer blocks RUM/CSP/fleet ingestion (KCDX-039), privileged commands carry `command_id` (0197), the ledger is reconciled. 0227 adds the missing piece — canaries. Every silent failure this remediation found raised NO exception (crons that never ran while Vercel reported healthy; notifications a constraint rejected and the error discarded; sessions that never expired), so there was nothing to alert on; `klimr_health()` asks about ABSENCE instead, across 9 subsystems. Verified by reproducing three of the session's real failures. **A guard that tested the wrong thing, found while building it:** all cron scheduling was wrapped in `exists(pg_extension where extname='pg_cron')`, false in the CI harness — so no migration's scheduling had EVER run in CI, and the jobs existed only where nothing verified them. Now `to_regprocedure(...)`; the replay registers all 10 jobs. **Still owed:** explicit product/operational/security/safety plane separation and alert routing. |
| KCDX-065 | P2 | G | Performance / Shell and Queue | Global shell and Queue refresh perform excessive work per view | ROOT-PERF-001 / — | **PARTIALLY FIXED** — shell collapsed to one RPC; fonts/Queue push owed | `chrome-data.ts` ran NINE serial reads plus a per-match COUNT loop on every page view for every signed-in member, to render a header. The loop existed for a good product reason (the NEXT chip must show a FULL roster so a half-empty morning match does not mask a full afternoon one) but implemented it by counting each candidate in turn — so the more upcoming matches a member had, the more queries their header cost, and **the most active members paid the most**. 0230's `chrome_data()` returns presence, teams, unread and the next full-roster match in ONE invoker-rights query with the roster test as a grouped join; verified it still skips an earlier half-empty match. `getTopBarData` also drops its `userId` parameter — the RPC uses `auth.uid()`, so passing an id alongside the session was a second source of truth about identity, which agrees in every test and disagrees once in production. **Still owed:** font transfer budget (6 families/styles), Queue version/head-push + adaptive poll, presence off the critical render, and the measurement pass the finding asks for first. |
| KCDX-066 | P2 | G | Accessibility / Overlays and targets | Overlay accessibility and mobile target sizes fall short | ROOT-A11Y-001 / — | **PARTIALLY FIXED** — shared primitive built and adopted once; targets/typography owed | `aria-modal='true'` tells a screen reader the page behind is inert; it does not make it so. Without a trap, Tab walked out of the dialog into controls the user could not see while the screen reader insisted they were in a modal — and on close focus fell to `document.body`, so a keyboard user lost their place and restarted from the top of the document every time. `useDialogA11y` (trap + restore + Escape) is the shared implementation, deliberately a hook not a `<Dialog>` so it can be adopted one dialog at a time; `join-waitlist-dialog` is the first, and a ratchet pins the 12 remaining. **The mobile menu was worse:** `aria-hidden` + `pointer-events-none` stops a mouse and nothing else, so every link stayed in the tab order and a keyboard user fell into an invisible menu — `aria-hidden` with focusable descendants IS the violation. Now `inert` (React 19). **Still owed:** 44px mobile targets and replacing meaningful 10px text with semantic tokens. |
| KCDX-067 | P2 | G | Maintainability / Module size | Giant server-action modules combine too many invariants and side effects | ROOT-MAINT-001 / — | **PARTIALLY FIXED** — one concern extracted, ratchet pinned; incremental by design | Characterised first, as the finding asks, and the honest result is that the files GREW: tournaments 2,606 → 2,684, queue 935 → 972, events 864 → 898. Every command moved into the database this week replaced application logic with an RPC call AND an explanation of what had been wrong — the invariants left, longer commentary arrived. That trade is defensible but it moves the cited metric the wrong way, and claiming improvement without saying so would be choosing the flattering measure. **One real extraction:** the four payment actions share a subject, an error vocabulary, and (since 0193) two locked commands, and nothing else in the module touches payment state — so `payment-actions.ts` removes a concern rather than relocating lines. 2,684 → 2,525. A vitest ratchet now pins all four modules so they can fall and cannot rise. The audit explicitly warns against a big-bang rewrite; this is one step, not a plan for the rest. |
| KCDX-068 | P2 | D1 | Logging / Privacy / Secrets | Error logging stores dynamic paths and large raw stack traces without central scrubbing | ROOT-LOG-001 / S2-SEC-013 | **FIXED** — awaiting 0198 paste | `lib/log-scrub.ts`: templates paths (`/q/XK4M2P` → `/q/:code`, `/gate/ABCD-EFGH-IJKL` → `/gate/:code`, UUID → `:id`), keeps query keys and drops values, pseudonymises UUIDs to a stable `id:xxxxxxxx` so correlation survives, redacts JWTs, Supabase keys, credential key/value pairs, emails, phones, geo pairs and Klimr codes, and bounds detail to 2,000 chars (was 6,000). Wired into ALL SIX writers: instrumentation, account/log-actions (browser-supplied), CSP report, app-diagnostics (Courtside over the public network), and two queue-trace paths. 10 tests seeded with real markers asserting BOTH that the marker is gone and that the actionable parts survive. Guardrail tripwire fails the build if any `error_logs` insert skips the scrubber. 0198 adds 30-day retention with a nightly prune. Access already correct: RLS with no policy denies `authenticated`, and 0196 removed the anon grant. **Bug found by the tests:** the invite-code regex matched digit groups inside UUIDs, destroying rather than pseudonymising them — caught only because the test asserted the pseudonym SURVIVED. |
